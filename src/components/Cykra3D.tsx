'use client'

/**
 * Cykra3D — Hero-Komponente.
 *
 * Tag 1: Stub. Die volle Spline + R3F + Lottie + detect-gpu Implementation
 * liegt in der VaultCity unter:
 *   05 Werkstatt-Gasse/001-cykrus-relaunch/Konzept/cykra3d-component.tsx
 *
 * Lukas wired sie sobald die GLB von Spline da ist.
 */
export default function Cykra3D() {
  return (
    <div
      aria-hidden
      className="pointer-events-none absolute inset-0 -z-10 bg-cykra-aurora opacity-70"
    />
  )
}
